settings.logAddedRecipes = true
settings.logRemovedRecipes = true
settings.logSkippedRecipes = false
settings.logErroringRecipes = true

var n = 1;
var id;

onEvent('recipes', event =>
{
    while (n < 70) {
      event.stonecutting(`chipped:amethyst_block_${n}`, '#chipped:amethyst_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:andesite_${n}`, '#chipped:andesite')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:ancient_debris_${n}`, '#chipped:ancient_debris')
      n++;
  };

    n = 1;
    while (n < 34) {
      event.stonecutting(`chipped:barrel_${n}`, '#chipped:barrel')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:basalt_${n}`, '#chipped:basalt')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:blackstone_${n}`, '#chipped:blackstone')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:blue_ice_${n}`, '#chipped:blue_ice')
      n++;
  };

    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:bone_block_${n}`, '#chipped:bone_block')
      n++;
  };

    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:bookshelf_${n}`, '#chipped:bookshelf')
      n++;
  };

    n = 1;
    while (n < 134) {
      event.stonecutting(`chipped:bricks_${n}`, '#chipped:bricks')
      n++;
  };

    n = 1;
    while (n < 25) {
      event.stonecutting(`chipped:brown_mushroom_block_${n}`, '#chipped:brown_mushroom_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:calcite_${n}`, '#chipped:calcite')
      n++;
  };

    n = 1;
    while (n < 23) {
      event.stonecutting(`chipped:carved_pumpkin_${n}`, '#chipped:carved_pumpkin')
      n++;
  };

    n = 1;
    while (n < 68) {
      event.stonecutting(`chipped:clay_${n}`, '#chipped:clay')
      n++;
  };

    n = 1;
    while (n < 68) {
      event.stonecutting(`chipped:coal_block_${n}`, '#chipped:coal_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:cobblestone_${n}`, '#chipped:cobblestone')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:crying_obsidian_${n}`, '#chipped:crying_obsidian')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:dark_prismarine_${n}`, '#chipped:dark_prismarine')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:deepslate_${n}`, '#chipped:deepslate')
      n++;
  };

    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:diamond_block_${n}`, '#chipped:diamond_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:diorite_${n}`, '#chipped:diorite')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:dirt_${n}`, '#chipped:dirt')
      n++;
  };

    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:dried_kelp_block_${n}`, '#chipped:dried_kelp_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:dripstone_block_${n}`, '#chipped:dripstone_block')
      n++;
  };

    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:emerald_block_${n}`, '#chipped:emerald_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:end_stone_${n}`, '#chipped:end_stone')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:blackstone_${n}`, '#chipped:blackstone')
      n++;
  };

    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:glowstone_${n}`, '#chipped:glowstone')
      n++;
  };

    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:gold_block_${n}`, '#chipped:gold_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:granite_${n}`, '#chipped:granite')
      n++;
  };

    n = 1;
    while (n < 8) {
      event.stonecutting(`chipped:gravel_${n}`, '#chipped:gravel')
      n++;
  };

    n = 1;
    while (n < 9) {
      event.stonecutting(`chipped:hay_block_${n}`, '#chipped:hay_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:ice_${n}`, '#chipped:ice')
      n++;
  };

    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:iron_bars_${n}`, '#chipped:iron_bars')
      n++;
  };

    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:iron_block_${n}`, '#chipped:iron_block')
      n++;
  };

    n = 1;
    while (n < 23) {
      event.stonecutting(`chipped:jack_o_lantern_${n}`, '#chipped:jack_o_lantern')
      n++;
  };

    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:ladder_${n}`, '#chipped:ladder')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:lapis_block_${n}`, '#chipped:lapis_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:basalt_${n}`, '#chipped:basalt')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:lodestone_${n}`, '#chipped:lodestone')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:magma_block_${n}`, '#chipped:magma_block')
      n++;
  };

    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:melon_${n}`, '#chipped:melon')
      n++;
  };

    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:moss_block_${n}`, '#chipped:moss_block')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:mossy_cobblestone_${n}`, '#chipped:mossy_cobblestone')
      n++;
  };

    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:mossy_stone_bricks_${n}`, '#chipped:mossy_stone_bricks')
      n++;
  };

    id = "mushroom_stem";
    n = 1;
    while (n < 8) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "nether_bricks";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "nether_wart_block";
    n = 1;
    while (n < 15) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "netherite_block";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "netherrack";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "obsidian";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "packed_ice";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "prismarine";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pumpkin";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purpur_block";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "quartz_block";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "raw_copper_block";
    n = 1;
    while (n < 62) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "raw_gold_block";
    n = 1;
    while (n < 62) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "raw_iron_block";
    n = 1;
    while (n < 62) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_mushroom_block";
    n = 1;
    while (n < 16) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_nether_bricks";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_sandstone";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "redstone_lamp";
    n = 1;
    while (n < 19) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "sand";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "sandstone";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "sea_lantern";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "shroomlight";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

  id = "smooth_stone";
  n = 1;
  while (n < 70) {
    event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
    n++;
  };

  id = "snow_block";
  n = 1;
  while (n < 70) {
    event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
    n++;
  };

    id = "soul_sand";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "sponge";
    n = 1;
    while (n < 7) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stone";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "tuff";
    n = 1;
    while (n < 70) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_wart_block";
    n = 1;
    while (n < 15) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "waxed_copper_block";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "waxed_exposed_copper";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "waxed_oxidized_copper";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "waxed_weathered_copper";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "waxed_copper_block";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "glass";
    n = 1;
    while (n < 102) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "glass_pane";
    n = 1;
    while (n < 102) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_stained_glass";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_stained_glass_pane";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "acacia_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "birch_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "crimson_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "dark_oak_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "jungle_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "oak_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "spruce_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_planks";
    n = 1;
    while (n < 42) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "acacia_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "birch_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "crimson_stem";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "dark_oak_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "jungle_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "oak_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "spruce_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_stem";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_acacia_log";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_birch_log";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_crimson_stem";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_dark_oak_log";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_jungle_log";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_oak_log";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_spruce_log";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "stripped_warped_stem";
    n = 1;
    while (n < 17) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "acacia_leaves";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "birch_leaves";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "dark_oak_leaves";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "jungle_leaves";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "oak_leaves";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "spruce_leaves";
    n = 1;
    while (n < 13) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_terracotta";
    n = 1;
    while (n < 67) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_glazed_terracotta";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_concrete";
    n = 1;
    while (n < 22) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_wool";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "white_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "orange_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "magenta_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_blue_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "yellow_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lime_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pink_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "gray_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "light_gray_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cyan_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "purple_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "blue_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "green_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "black_carpet";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "acacia_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "dark_oak_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "birch_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "jungle_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "oak_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "spruce_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "crimson_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_door";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "acacia_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "dark_oak_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "birch_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "jungle_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "oak_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "spruce_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "crimson_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_trapdoor";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "brown_mushroom";
    n = 1;
    while (n < 16) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "cobweb";
    n = 1;
    while (n < 11) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "crimson_fungus";
    n = 1;
    while (n < 16) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "crimson_roots";
    n = 1;
    while (n < 15) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "pointed_dripstone";
    n = 1;
    while (n < 14) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lily_pad";
    n = 1;
    while (n < 23) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "nether_sprouts";
    n = 1;
    while (n < 21) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "red_mushroom";
    n = 1;
    while (n < 16) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "vine";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_fungus";
    n = 1;
    while (n < 15) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "warped_roots";
    n = 1;
    while (n < 10) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "torch";
    n = 1;
    while (n < 10) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "redstone_torch";
    n = 1;
    while (n < 6) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:${id}`)
      n++;
  };

    id = "lantern";
    n = 1;
    while (n < 15) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:lantern`)
      n++;
  };

    id = "soul_lantern";
    n = 1;
    while (n < 12) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:soul_lantern`)
      n++;
  };

    id = "special_lantern";
    n = 1;
    while (n < 5) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:lantern`)
      n++;
  };

    id = "special_soul_lantern";
    n = 1;
    while (n < 5) {
      event.stonecutting(`chipped:${id}_${n}`, `#chipped:soul_lantern`)
      n++;
  };
})
